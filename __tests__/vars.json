{
    "vars": [
      {
        "name": "$SCRIPT_SECRET",
        "secret": "${{ secrets.SCRIPT_SECRET }}"
      },
      {
        "name": "$SCRIPT_SECRET2",
        "secret": "${{ secrets.SCRIPT_SECRET2 }}"
      },
      {
        "name": "$SCRIPT_SECRET3",
        "secret": "${{ secrets.SCRIPT_SECRET3 }}"
      },
      {
        "name": "$SCRIPT_SECRET4",
        "secret": "${{ secrets.SCRIPT_SECRET4 }}"
      }
    ]
}  